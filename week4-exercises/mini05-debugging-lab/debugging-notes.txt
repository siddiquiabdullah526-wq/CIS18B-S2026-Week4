The bug was that the loop utilized <= alerts.length, which caused the index to go out of bounds.
The way that I found this bug was by running it in debug mode, and I noticed that it crashed when i = alerts.length.
So, to fix it, all I did was change <= to < so that the loop only accesses indexes that are valid.
